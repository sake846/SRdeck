<UserControl x:Class="SRdeckPlugin.Wpf.GeoMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Background="{DynamicResource PanelBaseBrush}" Loaded="OnLoaded" Unloaded="OnUnloaded">
    <Grid ClipToBounds="True" SizeChanged="OnMapSizeChanged">
        <wv2:WebView2 x:Name="MapWebView" />
        <Border x:Name="StatusOverlay" Background="{DynamicResource PanelBaseBrush}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="18">
                <TextBlock Text="OpenStreetMap" Foreground="{DynamicResource LedYellowGreenBrush}" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Center" />
                <TextBlock x:Name="StatusText" Text="地図を初期化しています…" Foreground="{DynamicResource TextDimBrush}" FontSize="12" TextAlignment="Center" TextWrapping="Wrap" Margin="0,7,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
