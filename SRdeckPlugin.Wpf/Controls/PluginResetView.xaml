<UserControl x:Class="SRdeckPlugin.Wpf.Controls.PluginResetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Foreground="{DynamicResource TextPrimaryBrush}"
             FontSize="10">
    <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="プラグイン設定の初期化" Style="{DynamicResource PluginSecondarySectionHeaderStyle}" Margin="0,0,0,7" />
                <TextBlock Text="プラグインの各種動作設定（settings.json）を初期状態に戻します。" Style="{DynamicResource PluginSettingsHintStyle}" />
            </StackPanel>
            <Button Grid.Column="1" Content="設定を初期化" Style="{DynamicResource PluginButtonStyle}" Width="92" Height="26" VerticalAlignment="Center" Margin="16,0,0,0" Command="{Binding ResetPluginSettingsCommand}" />
    </Grid>
</UserControl>
