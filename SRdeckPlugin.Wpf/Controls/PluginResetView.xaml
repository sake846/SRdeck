<UserControl x:Class="SRdeckPlugin.Wpf.Controls.PluginResetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Foreground="{DynamicResource TextPrimaryBrush}"
             FontSize="10">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="プラグイン設定の初期化" Style="{DynamicResource PluginSettingsSectionHeaderStyle}" />
        <Grid Grid.Row="1" HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="プラグインの各種動作設定（settings.json）を初期状態に戻します。"
                       Style="{DynamicResource PluginSettingsDescriptionStyle}" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="設定を初期化" Style="{DynamicResource PluginButtonStyle}" Width="92" Height="26"
                    HorizontalAlignment="Left" VerticalAlignment="Center" Margin="16,0,0,0"
                    Command="{Binding ResetPluginSettingsCommand}" />
        </Grid>
    </Grid>
</UserControl>
